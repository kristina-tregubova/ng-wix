import { DateToIsoPipe } from './date-to-iso.pipe';

describe('DateToIsoPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToIsoPipe();
    expect(pipe).toBeTruthy();
  });
});
